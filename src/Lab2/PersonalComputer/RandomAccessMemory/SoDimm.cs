namespace Itmo.ObjectOrientedProgramming.Lab2.PersonalComputer.RandomAccessMemory;

public class SoDimm : RamFormFactor
{
    public SoDimm(int memorySize, int powerConsumption)
        : base(memorySize, powerConsumption)
    {
    }
}